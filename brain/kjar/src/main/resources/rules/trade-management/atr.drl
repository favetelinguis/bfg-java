package rules.tradeManagement;

import org.trading.model.*;
import org.trading.event.*;

dialect  "mvel"

rule "Atr trigger"
    when
      $o: Atr($e: epic, $l: level) from entry-point "ATR"
    then
      System.out.println("DROOLS ATR " + $l);
end

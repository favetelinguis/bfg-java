package rules.tradeManagement;

import org.trading.model.*;
import org.trading.event.*;

dialect  "mvel"

rule "Opu trigger"
    when
      $o: Opu($e: epic) from entry-point "OPU"
    then
      System.out.println("DROOLS OPU");
end

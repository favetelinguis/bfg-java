package rules.tradeManagement;

import org.trading.model.*;
import org.trading.event.*;
import org.trading.ChannelIds;

dialect  "mvel"

rule "On each ask event trigger"
    when
      $a: Ask() from entry-point "ASK"
    then
      channels[ChannelIds.OPEN_WORKING_ORDER].send("open");
end
